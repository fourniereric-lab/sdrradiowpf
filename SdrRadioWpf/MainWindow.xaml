<Window x:Class="SdrRadioWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SdrRadioWpf.ViewModels"
        mc:Ignorable="d"
        Title="SDR Radio" Height="520" Width="700"
        Background="#1E1E2E"
        Foreground="White"
        Closing="Window_Closing">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#585B70"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#585B70"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="BorderBrush" Value="#89B4FA"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#F38BA8"/>
        </Style>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A6ADC8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
        <Style x:Key="GroupBorder" TargetType="Border">
            <Setter Property="Background" Value="#181825"/>
            <Setter Property="BorderBrush" Value="#313244"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <TextBlock Grid.Row="0" Text="📻 SDR Radio" FontSize="22" FontWeight="Bold"
                   Foreground="#89B4FA" Margin="0,0,0,12"/>

        <!-- Device selection -->
        <Border Grid.Row="1" Style="{StaticResource GroupBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="5" Style="{StaticResource SectionLabel}" Text="DEVICE"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Device:" Margin="0,0,8,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1"
                          ItemsSource="{Binding Devices}"
                          SelectedIndex="{Binding SelectedDeviceIndex}"
                          Margin="0,0,8,0"/>
                <Label Grid.Row="1" Grid.Column="2" Content="PPM:" Margin="0,0,4,0"/>
                <TextBox Grid.Row="1" Grid.Column="3" Width="50"
                         Text="{Binding PpmCorrection, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,0"/>
                <Button Grid.Row="1" Grid.Column="4" Content="🔄 Refresh"
                        Command="{Binding RefreshDevicesCommand}"
                        Background="#A6E3A1" Foreground="#1E1E2E"/>
            </Grid>
        </Border>

        <!-- Frequency -->
        <Border Grid.Row="2" Style="{StaticResource GroupBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="6" Style="{StaticResource SectionLabel}" Text="FREQUENCY"/>
                <Label Grid.Row="1" Grid.Column="0" Content="MHz:" Margin="0,0,8,0"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding FrequencyMHz, StringFormat=F3, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="18" FontWeight="Bold" Foreground="#A6E3A1"
                         Margin="0,0,8,0"/>
                <Label Grid.Row="1" Grid.Column="2" Content="Step:" Margin="0,0,4,0"/>
                <ComboBox Grid.Row="1" Grid.Column="3" Width="80"
                          SelectedValue="{Binding FrequencyStepMHz}"
                          SelectedValuePath="Tag" Margin="0,0,8,0">
                    <ComboBoxItem Content="0.001 MHz" Tag="0.001"/>
                    <ComboBoxItem Content="0.01 MHz"  Tag="0.01"/>
                    <ComboBoxItem Content="0.1 MHz"   Tag="0.1" IsSelected="True"/>
                    <ComboBoxItem Content="1 MHz"     Tag="1"/>
                    <ComboBoxItem Content="5 MHz"     Tag="5"/>
                </ComboBox>
                <Button Grid.Row="1" Grid.Column="4" Content="▲" Width="32"
                        Command="{Binding StepUpCommand}" Margin="0,0,4,0"/>
                <Button Grid.Row="1" Grid.Column="5" Content="▼" Width="32"
                        Command="{Binding StepDownCommand}"/>
            </Grid>
        </Border>

        <!-- Demodulation and volume -->
        <Border Grid.Row="3" Style="{StaticResource GroupBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="6" Style="{StaticResource SectionLabel}" Text="DEMODULATION &amp; AUDIO"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Mode:" Margin="0,0,8,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Width="100"
                          ItemsSource="{Binding AvailableModes}"
                          SelectedItem="{Binding SelectedMode}"
                          Margin="0,0,0,0"/>
                <Label Grid.Row="1" Grid.Column="3" Content="🔊" Margin="0,0,4,0"/>
                <Slider Grid.Row="1" Grid.Column="4"
                        Minimum="0" Maximum="100"
                        Value="{Binding Volume}"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0"/>
                <Label Grid.Row="1" Grid.Column="5"
                       Content="{Binding Volume, StringFormat={}{0:F0}%}"
                       Width="40"/>
            </Grid>
        </Border>

        <!-- Signal level -->
        <Border Grid.Row="4" Style="{StaticResource GroupBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Style="{StaticResource SectionLabel}" Text="SIGNAL LEVEL"/>
                <ProgressBar Grid.Row="1" Height="18"
                             Minimum="0" Maximum="100"
                             Value="{Binding SignalLevel, Mode=OneWay}"
                             Foreground="#A6E3A1" Background="#313244"
                             BorderBrush="#585B70"/>
            </Grid>
        </Border>

        <!-- Status log -->
        <Border Grid.Row="5" Style="{StaticResource GroupBorder}" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Style="{StaticResource SectionLabel}" Text="STATUS"/>
                <TextBlock Grid.Row="1"
                           Text="{Binding StatusMessage}"
                           Foreground="#CDD6F4"
                           TextWrapping="Wrap"
                           FontFamily="Consolas"
                           FontSize="12"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Control buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="▶ Start" Width="120" Margin="0,0,16,0"
                    Command="{Binding StartCommand}"/>
            <Button Content="■ Stop" Width="120"
                    Style="{StaticResource StopButtonStyle}"
                    Command="{Binding StopCommand}"/>
        </StackPanel>
    </Grid>
</Window>
